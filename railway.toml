[build]
builder = "NIXPACKS"

[build.nixpacks]
plan = "python"

[deploy]
startCommand = "cd backend && python -m uvicorn main:socket_app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
